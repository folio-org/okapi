{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "http://okapi/deployment_descriptor#",
  "title": "DeploymentDescriptor",
  "type": [ "object", "null" ],
  "properties": {
    "srvcId": {"type": "string"},
    "instId": {"type": "string"},
    "url": {"type": ["string", "null"]},
    "descriptorXX" : { "$ref": "LaunchDescriptor.json" },

    "descriptor" : {
      "description" : "For some reason extracting this to LaunchDescriptor.json fails here, although I can use it in moduleDescriptor",
      "type": ["object", "null"],
      "properties": {
        "exec": {"type": ["string", "null"]},
        "cmdlineStart": {"type": ["string", "null"]},
        "cmdlineStop": {"type": ["string", "null"]}
      }
    }
  },
  "required": ["srvcId"]
}
